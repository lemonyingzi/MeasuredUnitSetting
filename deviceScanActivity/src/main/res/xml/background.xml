<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

	<!-- 边框  虚线之间的间隔  dashWidth和dashGap有一个为0，则为实线-->
    <stroke
        android:width="0.01dp"
        android:color="#F5F5F5"
        android:dashWidth="0dp"
        android:dashGap="0dp"/>
	<!-- 填充 -->
    <solid
        android:color="#F5F5F5"/><!-- 填充的颜色 -->
    <padding
        android:bottom="1dp"
        android:left="0dp"
        android:right="0dp"
        android:top="0dp" />
    
 
</shape>